# Configuración mínima del proyecto local de Supabase CLI.
# El project_id se establece al ejecutar `supabase link --project-ref <ref>`.
project_id = "desarrolladora-california"

[db]
major_version = 15
